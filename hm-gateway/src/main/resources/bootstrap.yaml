spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: "***" # ip+port
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml
