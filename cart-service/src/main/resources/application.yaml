server:
  port: 8082
feign:
  okhttp:
    enabled: true # enable OKHttp
  sentinel:
    enabled: true # enable feign-sentinel
hm:
  db:
    database: hm-cart
  swagger:
    title: "Cart Service API Documentation"
    package: com.hmall.cart.controller
spring:
  cloud:
    sentinel:
      transport:
        dashboard: localhost:8090
      http-method-specify: true
